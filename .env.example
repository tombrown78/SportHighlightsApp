# Environment Configuration for Sports Highlights App
# Copy this file to .env and fill in your values

# Database
POSTGRES_USER=sports
POSTGRES_PASSWORD=sports123
POSTGRES_DB=sports_highlights

# Processing Mode: "local" (use GPU) or "cloud" (use cloud APIs)
PROCESSING_MODE=local

# Cloud Provider (only needed if PROCESSING_MODE=cloud)
# Options: replicate, roboflow, runpod
CLOUD_PROVIDER=

# Cloud API Keys (only needed if using cloud processing)
REPLICATE_API_TOKEN=
ROBOFLOW_API_KEY=
RUNPOD_API_KEY=

# Optional: Google Video Intelligence (for basic detection fallback)
GOOGLE_APPLICATION_CREDENTIALS=
