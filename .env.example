# Environment Configuration for Sports Highlights App
# Copy this file to .env and fill in your values

# Database
POSTGRES_USER=sports
POSTGRES_PASSWORD=sports123
POSTGRES_DB=sports_highlights

# Processing Mode: "local" (use GPU) or "cloud" (use cloud APIs)
PROCESSING_MODE=local

# Cloud Provider (only needed if PROCESSING_MODE=cloud)
# Options: replicate, roboflow, runpod
CLOUD_PROVIDER=

# Cloud API Keys (only needed if using cloud processing)
REPLICATE_API_TOKEN=
ROBOFLOW_API_KEY=
RUNPOD_API_KEY=

# YouTube Authentication (optional - for private/age-restricted videos)
# Method 1: OAuth2 (recommended) - set to true and follow browser prompts on first use
YOUTUBE_USE_OAUTH=false

# Method 2: Cookies file (most reliable)
# Export cookies from your browser using a browser extension like "Get cookies.txt"
# Save the file to: backend/videos/youtube_cookies.txt (or mount it in Docker)
# The cookies file will be automatically detected

# Method 3: Username/Password (legacy - not recommended due to 2FA issues)
# YOUTUBE_USERNAME=your_email@gmail.com
# YOUTUBE_PASSWORD=your_password

# Optional: Google Video Intelligence (for basic detection fallback)
GOOGLE_APPLICATION_CREDENTIALS=
