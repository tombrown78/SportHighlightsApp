# Environment Configuration for Sports Highlights App
# Copy this file to .env and fill in your values

# ============ DATABASE ============
POSTGRES_USER=sports
POSTGRES_PASSWORD=sports123
POSTGRES_DB=sports_highlights

# ============ PROCESSING ============
# Processing Mode: "local" (use GPU) or "cloud" (use cloud APIs)
PROCESSING_MODE=local

# ============ ROBOFLOW (Optional but Recommended) ============
# Enables basketball-specific detection for better accuracy
# Get a free API key at: https://roboflow.com/
# 
# Benefits:
# - Basketball-specific ball detection (more accurate than COCO)
# - Hoop/rim detection
# - Shot made/missed classification
#
ROBOFLOW_API_KEY=

# ============ CLOUD PROVIDERS (Only if PROCESSING_MODE=cloud) ============
# Options: replicate, roboflow, runpod
CLOUD_PROVIDER=
REPLICATE_API_TOKEN=
RUNPOD_API_KEY=

# ============ YOUTUBE AUTHENTICATION (Optional) ============
# For accessing private or age-restricted YouTube videos

# Method 1: OAuth2 (recommended for personal use)
# Set to true and follow browser prompts on first use
YOUTUBE_USE_OAUTH=false

# Method 2: Cookies file (most reliable for automation)
# Export cookies from your browser using "Get cookies.txt" extension
# Save the file to: ./youtube_cookies.txt (mounted automatically)
# The cookies file will be automatically detected

# Method 3: Username/Password (legacy - not recommended due to 2FA)
# YOUTUBE_USERNAME=your_email@gmail.com
# YOUTUBE_PASSWORD=your_password

# ============ ADVANCED SETTINGS ============
# These have sensible defaults but can be customized

# Detection confidence thresholds (0.0 - 1.0)
# DETECTION_CONFIDENCE=0.45
# TRACKING_CONFIDENCE=0.5
# OCR_CONFIDENCE=0.5

# Track filtering (to reduce false player counts)
# MIN_TRACK_DURATION_SECONDS=2.0
# MIN_TRACK_DETECTIONS=30

# Video limits
# MAX_VIDEO_SIZE_MB=2000
